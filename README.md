# SupMail

:e-mail: A web application to see, reply, has its mails received in a database

# Install
```shell
npm i || yarn install
npm run start || yarn start
```

# Configuration
```json
{
    "database": {
        "host": "",
        "database": "",
        "user": "",
        "password": "",
    }
}
```
