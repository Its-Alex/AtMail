# SupMail
---
:e-mail: A web application to see, reply, has its mails received in a database

# Install
* npm || yarn install
* npm || yarn start

# Configuration
```json
{
    "database": {
        "host": "",
        "database": "",
        "user": "",
        "password": "",
    }
}
```
