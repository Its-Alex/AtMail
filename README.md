# SupMail
---
A web application to see, reply, has its mails received in a database

# Install
* npm || yarn install
* npm || yarn start

# Configuration
```json
{
    "databse": {
        "host": "",
        "database": "",
        "user": "",
        "password": "",
    }
}
```